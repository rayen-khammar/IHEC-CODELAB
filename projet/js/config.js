// Change this to your backend base URL
// Example: "http://localhost:8000" or "https://your-api.com"
export const API_BASE = "http://localhost:8000";

// Polling intervals (ms)
export const POLL_OVERVIEW_MS = 15000;
export const POLL_ALERTS_MS = 10000;
export const POLL_STOCK_MS = 15000;

// Timeouts
export const FETCH_TIMEOUT_MS = 9000;

// Optional: set to true to use mock fallback when API fails
export const ALLOW_MOCK_FALLBACK = true;
